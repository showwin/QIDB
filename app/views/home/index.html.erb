<div class="row">
  <div class="col-xs-12">
    <%= form_tag search_path, {method: "get"} do %>
      <h3><b>検索キーワード</b></h3>
      <div class="input-group">
        <%= text_field_tag 'query', params['query'], :class => 'form-control input', :id => "btn-input", :placeholder => '大動脈バルーンパンピング法　人工心肺' %>
        <span class="input-group-btn">
          <%= submit_tag "　検　索　", :class => "btn btn-info btn", :id => "btn-chat" %>
        </span>
      </div>
    <% end %>
  </div>
</div>

<%= link_to 'Download CSV', download_csv_path(format: :csv) %>

<div class="panel-body">
  <dl>
  <% if !@results.nil? %>
    <% @results.each do |r| %>
      <dt><a href= <%= "/definitions/#{r._id.to_s}" %>>  指標群: <%= r.group %> </a></dt>
      <dd>名称: <%= r.name %></dd>
      <dd>意義: <%= r.meaning %></dd>
      <hr>
    <% end %>
  <% end %>
  </dl>
</div>
